description: Validation error — dữ liệu không hợp lệ
content:
  application/json:
    schema:
      $ref: '../schemas/ErrorResponse.yaml'
    example:
      code: VALIDATION_ERROR
      message: "Dữ liệu không hợp lệ"
      details:
        - field: "url"
          message: "Không đúng định dạng URI"
        - field: "title"
          message: "Không được để trống"
