description: Lỗi server nội bộ - Mã lỗi 500
content:
  application/json:
      schema:
        $ref: "../schemas/core/StandardError.yaml"
      example:
        success: false
        error:
          code: 500
          message: "Lỗi server nội bộ, vui lòng thử lại sau."
          data: null
