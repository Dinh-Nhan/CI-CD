# StandardError được sử dụng cho tất cả response lỗi
type: object
required:
  - success
  - error
properties:
  success:
    type: boolean
    description: "Luôn là false khi có lỗi"
    example: false
  error:
    $ref: './ErrorDetail.yaml'

